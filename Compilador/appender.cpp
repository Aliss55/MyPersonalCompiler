#include <bits/stdc++.h>
// librerias
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string word;
    while (cin >> word)
    {
        cout << word << endl;
    }
    return 0;
}