#include <bits/stdc++.h>
// librerias
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int counter = 0;
    string word;
    while (cin >> word)
    {
        counter++;
    }
    cout << counter<<endl;
    return 0;
}